name: Anima Build and Deploy action
on:
  push:
    branches:
      - anima-v2-deploy
  workflow_dispatch:
    inputs:
      release_name:
        description: 'Version Name (e.g. v2.0.0)'
        required: true
        default: 'v2.0.0'
      is_prerelease:
        description: 'Is this a Pre-release?'
        type: boolean
        default: false

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Qt (MSVC Native)
        uses: jurplel/install-qt-action@v3
        with:
          version: '6.8.3'
          arch: 'win64_msvc2019_64'

      - name: Setup MSVC Compiler
        uses: ilammy/msvc-dev-cmd@v1

      - name: Configure CMake
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build Project
        run: cmake --build build --config Release

      - name: Bundle DLLs
        run: |
          windeployqt --qmldir . build/Release/appAnima_Pro.exe
          # MSVC puts the .exe inside a Release folder, so we updated the path here!

      - name: Create ZIP
        uses: thedoctor0/zip-release@main
        with:
          type: 'zip'
          directory: 'build/Release'
          filename: 'Anima-v2.zip'

      - name: Save Private Test Build
        uses: actions/upload-artifact@v4
        with:
          name: Private-Testing-Build
          path: build/Release/Anima-v2.zip

      - name: Create Official Release
        if: github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.event.inputs.release_name }}
          name: Anima ${{ github.event.inputs.release_name }}
          body_path: CHANGELOG.md
          prerelease: ${{ github.event.inputs.is_prerelease }}
          files: build/Release/Anima-v2.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}